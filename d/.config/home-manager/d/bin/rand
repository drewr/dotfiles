#! /usr/bin/env nix-shell
#! nix-shell -i perl -p perl perlPackages.MathRandomSecure perlPackages.ModernPerl

use Math::Random::Secure qw(irand);
use Modern::Perl;

our $count = shift @ARGV || 0;
our $offset = shift @ARGV || 10;

say irand($offset)+$count
