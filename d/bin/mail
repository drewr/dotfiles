#!/bin/bash

unset GNOME_KEYRING_CONTROL
unset GNOME_KEYRING_PID

msmtp=/usr/bin/msmtp
tmp=$(mktemp)
cat >$tmp

if egrep -q "^From:.*drew.raines@elas" $tmp; then
  msmtp="$msmtp -a drewres"
fi

$msmtp "$@" <$tmp && rm $tmp
