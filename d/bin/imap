#!/bin/zsh

source ~/perl5/perlbrew/etc/bashrc

while true; do
  if online; then 
    mbsync -c <(ttexpand <~/.mbsyncrc) aaraines
    echo \* finished at $(date)
    sleep 900
  else
    echo \* no imap available at $(date)
    sleep 60
  fi
done
