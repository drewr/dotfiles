#!/bin/zsh

source ~/perl5/perlbrew/etc/bashrc

while true; do
  if online; then 
    mbsync -a -qq -c <(ttexpand <~/.mbsyncrc) 
    echo \* finished at $(date)
    sleep 300
  else
    echo \* no imap available at $(date)
    sleep 60
  fi
done
